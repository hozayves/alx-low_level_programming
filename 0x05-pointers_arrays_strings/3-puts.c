#include "main.h"
/**
 * _puts - function definition
 *
 * Description: A function that prints a string, followed by new line
 * @str: parameter
 *
 * Return: result
 */
void _puts(char *str)
{
	puts(str);
}

#include "main.h"
#include <string.h>
/**
 * _strlen - function defition
 *
 * Description: A function that returs the length of a string
 * @s: parameter
 *
 * Return: result
 */
int _strlen(char *s)
{
	return (strlen(s));
}
